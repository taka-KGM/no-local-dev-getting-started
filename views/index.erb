<div class="container">

  <table class="table table-striped table-bordered">
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Email</th>
    </tr>

    <% @tests.each do |test| %>
      <tr>
        <td><%= test.id %></td>
        <td><%= test.name %></td>
        <td><%= test.createddate %></td>
      </tr>
    <% end %>
  </table>

</div>
